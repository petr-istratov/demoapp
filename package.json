{
  "name": "demoapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cd api && npm run test:unit",
    "seed": "cd api && npm run db:seed:all",
    "server": "cd api && npm install && npm run dev",
    "client": "cd ui && npm install && npm start",
    "start": "concurrently \"npm run client\" \"npm run server\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
